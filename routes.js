const HOME = "/";
const SUBMIT = "/submit";
const VIEW = "/view";
const USERS = "/users";
const REGISTER = "/register";
const LOGIN = "/login";
const LOGOUT = "/logout";

const routes = {
  home: HOME,
  submit: SUBMIT,
  view: VIEW,
  users: USERS,
  register: REGISTER,
  login: LOGIN,
  logout: LOGOUT,
};

module.exports = routes;
